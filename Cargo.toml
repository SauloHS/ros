[workspace]
resolver = "2"
members = []

[package]
name = "ros"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader = "0.11"

[build-dependencies]
kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }